proxmox_api_url          = "Replace with your Proxmox API URL"
proxmox_api_token_id     = "pformat of username@authtype!api_keyname e.g. packer@pam!packer_api"
proxmox_api_token_secret = "Replace with your Proxmox API token secret" # pragma: allowlist secret
proxmox_node             = "Replace with your Proxmox node name"
storage_pool             = "Replace with your Proxmox storage pool name"

# rename the file to proxmox.pkrvars.hcl
# do not commit the file to the repository
